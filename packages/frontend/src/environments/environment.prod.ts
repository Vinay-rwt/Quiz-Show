export const environment = {
  production: true,
  // Set this to your production API URL at deploy time.
  // In most deployments (same-domain reverse proxy), use an empty string
  // so /api/* requests go to the same origin with no CORS needed.
  apiBase: '',
};
